# MySQL 공식 이미지 사용 (가장 간단하고 안정적)
FROM mysql:8.0

# 환경 변수 설정
ENV MYSQL_ROOT_PASSWORD=rootpassword
ENV MYSQL_DATABASE=wwe_db
ENV MYSQL_USER=wwe_user
ENV MYSQL_PASSWORD=wwe_password

# 포트 노출
EXPOSE 3306

